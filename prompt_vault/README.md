# Prompt Vault (macOS)

Prompt Vault is being rewritten as a macOS desktop app based on Electron + TypeScript.

## Quick start

```bash
cd prompt_vault
npm install
npm run dev
```

## Build macOS package

```bash
cd prompt_vault
npm run dist:mac
```

This rewrite intentionally drops compatibility with the previous Python implementation.
